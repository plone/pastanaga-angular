export class Selectable {
    // Element identifier
    id: string;
    // Text to display in screen
    name: string;
    // Element selection holder
    selected?: boolean;
    disabled?: boolean;
}
