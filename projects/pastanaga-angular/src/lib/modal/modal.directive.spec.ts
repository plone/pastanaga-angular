import { ModalTitleDirective } from './modal.directive';

describe('ModalDirectives', () => {
    it('should create an instance', () => {
        const directive = new ModalTitleDirective();
        expect(directive).toBeTruthy();
    });
});
